{
  "name": "@contentflow/frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Run: npm run dev --workspace=apps/frontend'",
    "build": "npm run build --workspace=apps/frontend",
    "dev:cms": "npm run develop --workspace=packages/cms"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
